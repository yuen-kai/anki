[build]
# override any global customizations, as the gradle plugin requires a specific path
target-dir = "target"

[env]
DESCRIPTORS_BIN = { value = "../../out/rslib/proto/descriptors.bin", relative = true }
STRINGS_JSON_ANKIDROID = { value = "../../out/strings.json", relative = true }
PROTOC = { value = "../../out/extracted/protoc/bin/protoc", relative = true }
GENERATED_BACKEND_DIR = { value = "rsdroid/build/generated/source/backend", relative = true }
