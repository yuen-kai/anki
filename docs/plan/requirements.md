# Speedrun: requirements ledger

> Every stated requirement, tracked to verified done. Reconcile this against the build before claiming any batch complete (see the `requirements-tracking` rule). Status: `todo` / `building` / `done` / `superseded`. "done" needs a test, a verified run, or a concrete file ref, not an assertion.

## Mastery progression (from the restatement, user-confirmed)

| #  | Requirement (observable)                                           | Status | Evidence                                                                              |
| :- | :----------------------------------------------------------------- | :----- | :------------------------------------------------------------------------------------ |
| P1 | Cards sorted by the same AAMC hierarchy the scaffold uses          | done   | `topic_grouped.rs` groups by taxonomy topic; scaffold options come from the same tree |
| P2 | Track which hierarchy areas the user has learned (per-topic state) | done   | `progression.rs` config map; `get_speedrun_progress` RPC. (Viewing it = U3.)          |
| P3 | Blocked section: learn one hierarchy block at a time               | done   | `select_blocked_or_mixed`; test `blocked_phase_serves_one_learning_block_then_mixes`  |
| P4 | Once a block is learned, mix it into the interleaved pool          | done   | same; blocked→mixed transition                                                        |
| P5 | Per-topic 4 states: learning, practicing, hierarchy, mastering     | done   | `progression.rs` state machine + tests                                                |
| P6 | Upgrade the cards to match the state                               | done   | `resolve_card_mode` + 4 card modes                                                    |
| P7 | Practicing: practice the concept                                   | done   | `concept_practice` mode (templates + tests)                                           |
| P8 | Hierarchy: practice problems using the scaffold                    | done   | `application_scaffolded` mode                                                         |
| P9 | Mastering: practice problems without the scaffold                  | done   | `application_unscaffolded` mode (scaffold fades)                                      |

## Recent UX asks

| #  | Requirement (observable)                                                                                                                                                                                                    | Status | Evidence / owner                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| :- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U1 | Show the topic's hierarchy (breadcrumb) on the card, at least when learning                                                                                                                                                 | done   | `GetSpeedrunCardContext` RPC + `taxonomy::topic_path_labels`; reviewer injects `window.speedrunTopicPath`; templates render it; tests: rust `topic_path_labels_walks_root_to_leaf`, node breadcrumb checks (184), qt 53                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| U2 | Merge the Learn and Practice buttons into one                                                                                                                                                                               | done   | `overview.py` single Study button (`tr.studying_study()` -> `_start_session()`, state-aware queue w/ fallback); D20 superseded by D30; qt `test_speedrun.py` (53)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| U3 | A UI listing all topics and which of the 4 stages each is at                                                                                                                                                                | done   | Topics section on the dashboard: `get_speedrun_progress` (now carries the hierarchy `path`, web-exposed) → `buildTopicsView` groups by category + a per-topic 4-stage mastery track + ladder summary. Tests: `lib.test.ts` (3), rust `progress_lists_covered_topics_with_states`                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| U4 | `frontend-design` polish pass on the Speedrun UI (dashboard, cards, Study entry, topics view)                                                                                                                               | done   | Unified the whole surface on one product accent (the cards' teal, theme-aware) across dashboard tiles + topics ladder; fixed the Study button losing its primary style after the merge (`overview.scss` `#study`); window title `Speedrun: <deck>`. Build + svelte/ts check + vitest green. Live visual QA pass still open ([B029](backlog.md#b029): no browser in this sandbox)                                                                                                                                                                                                                                                                                                                                            |
| U5 | Scores dashboard reachable within the app, not the topbar                                                                                                                                                                   | done   | [D34](decisions.md#d34): a **Scores** action on the deck overview's bottom bar (`overview.py`, `tr.studying_scores()`); the Tools-menu entry removed from `main.py`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| U6 | Restructure the Speedrun visuals from scratch: the teal/clinical polish (U4) read as ugly/generic, so give the product a distinct, non-AI-generic identity across every surface (dashboard, cards, study screen, deck list) | done   | New "score instrument" identity: cool paper + charcoal ink + one marigold signal (teal dropped), a monospace readout label system, and a drawn uncertainty **gauge** as the signature (Readiness on the real 472-528 scale). Applied to `speedrun/templates/{concept,application}.css` + `application_front.html`, `speedrun-dashboard/*` (+ gauge helpers in `lib.ts`), `overview.scss`, `deckbrowser.scss`. Verified: headless-Chromium before/after screenshots (light + dark + mobile); `just lint` green (svelte/ts/eslint/mypy/ruff/clippy); `test-ts` (vitest) + 184-check card-mode suite green. Fixed a pre-existing `overview.py` mypy shadow bug in passing. Logged in [design-iterations](design-iterations.md) |

## App shell overhaul: modern flow, Anki-unrecognizable (2026-07-01)

> User: "Make a decks/add screen that is modern (think quizlet). ... redo all of the top bar and bottom bar to follow a modern app flow. Completely redo how it is laid out by building a completely new visual layer on top. Anki should be unrecognizable as anki." + "rethink the user flow of the menu; just make sure that all the things the user currently can do are present." Hard constraint: **zero dropped capabilities.**

| #  | Requirement (observable)                                                                                                                                                                                                                                                                                                  | Status | Evidence / owner                                       |
| :- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----- | :----------------------------------------------------- |
| A1 | New top app bar: brand + primary nav (Decks/Browse/Stats, active state) + prominent Create + Sync + overflow menu; reads as a modern app, not Anki                                                                                                                                                                        | done   | `toolbar.py`, `toolbar.scss`, `toolbar.ts`             |
| A2 | `+ Create` consolidates create actions: Add cards, Create deck, Import file, Get shared                                                                                                                                                                                                                                   | done   | `toolbar.py` `_create_menu`                            |
| A3 | Overflow `⋯` menu surfaces **every** native-menubar action in-window (Undo/Redo, Switch profile, Import, Export, Create/Open backup, Exit, Study deck, Create filtered, Empty cards, Note types, Add-ons, DB check, Media check, Check updates, Preferences, Fullscreen, Zoom in/out/reset, Documentation, Donate, About) | done   | `toolbar.py` `_app_menu` (reuses native `QAction`s)    |
| A4 | Modern decks/home screen; preserves open, shift-select, per-deck options (rename/options/export/delete), collapse/expand, drag-drop reparent, studied-today                                                                                                                                                               | done   | `deckbrowser.py`, `deckbrowser.scss`, `deckbrowser.ts` |
| A5 | Modern deck study (overview) screen; preserves Study + Options/Scores/Custom study/Rebuild·Empty/Unbury/Description                                                                                                                                                                                                       | done   | `overview.py`, `overview.scss`                         |
| A6 | Modern bottom bar across deck list / overview / reviewer; reviewer keeps Edit/More/answer buttons/timer, restyled (Again=clay, Good/Easy=signal, Show Answer=primary)                                                                                                                                                     | done   | `toolbar-bottom.scss`, `reviewer-bottom.scss`          |
| A7 | One cohesive visual layer extending the "score instrument" identity (marigold/paper/charcoal/mono); theme-aware light+dark, responsive, visible focus, reduced-motion respected                                                                                                                                           | done   | all of the above                                       |

**Verification (2026-07-01).** `just check` functional gates green (mypy, ruff-lint, eslint, svelte-check, typescript, vitest, rust tests); my touched files pass `ruff format` + `dprint`. Remaining `just check` failures are pre-existing/environmental only: format drift in untouched `pylib`/rust/`scheduler.proto`/`.svelte`, the contributors minilint, and the installer pytest hitting a sandbox `PermissionError` on `~/Library/Caches`. Visuals verified by rendering the exact emitted markup against the compiled CSS in headless Chrome, light + dark, for every surface (`out/uiqa/*.png`). All prior capabilities preserved: top-bar nav + `top_toolbar_did_init_links` hook, sync `#sync`/`updateSyncColor`, deck open/shift-select/options/collapse/drag-drop/studied-today, overview Study + bottom actions, reviewer Edit/More/answer/timer, and every native-menu action via the overflow menu.

## Three scores: Memory / Performance / Readiness (brief §4, [D33](decisions.md#d33))

| #  | Requirement (observable)                                                                                             | Status | Evidence                                                                                                                                                                                                    |
| :- | :------------------------------------------------------------------------------------------------------------------- | :----- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1 | All three scores built and shown separately, never blended into one number                                           | done   | Memory + Performance + Readiness all live on the dashboard as separate tiles; no "overall" number anywhere. (Subagents were billing-blocked; built directly.)                                               |
| S2 | Memory: chance of recalling a taught fact                                                                            | done   | `memory_score.rs` (FSRS retrievability), live tile                                                                                                                                                          |
| S3 | Performance: chance of getting a new exam-style question right                                                       | done   | `performance_score.rs`: exam-weight-weighted accuracy over `SpeedrunApplication` attempts, distinct from Memory; 6 rust tests + 2 python e2e; live tile                                                     |
| S4 | Readiness: projected 472-528 score with a range + a confidence note tied to coverage                                 | done   | `readiness_score.rs`: Performance projected onto 472-528, coverage-widened range; reasons lead with "covered X% of topics"; 4 rust tests + 2 python e2e; live tile (points format)                          |
| S5 | Every score carries the full envelope: estimate, range, coverage %, how-sure, updated-at, main reasons, give-up rule | done   | shared `ScoreEnvelope` (Rust + proto + TS interface) rendered by one `ScoreTile`; `format` hint (ratio/points) so Readiness renders as whole points                                                         |
| S6 | Give-up rule written down; show no score when data is thin                                                           | done   | Memory: `graded_reviews >= 200 AND coverage >= 50%`; Performance: `>= 30 application attempts AND >= 50% coverage`; Readiness inherits Performance. All abstain with a reason naming the shortfall (tested) |

## Correctness fixes from the progression review (binding)

| #  | Requirement                                                                               | Status | Ref                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| :- | :---------------------------------------------------------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 | Reset `window.speedrunCardMode` between cards (no leak)                                   | done   | [B027](backlog.md#b027) fixed: `card_context_inject_script` always resets both globals; qt test `test_nothing_to_inject_resets_both_globals`                                                                                                                                                                                                                                                                                                                   |
| F2 | `speedrun_record_answer` ignores non-Speedrun note types                                  | done   | [B028](backlog.md#b028) fixed: early-return on `note_kind == Other`; rust test `record_answer_ignores_non_speedrun_note_kind`                                                                                                                                                                                                                                                                                                                                  |
| F3 | The single Study flow must not starve graduated topics' due reviews                       | done   | [B026](backlog.md#b026) fixed: blocked phase also serves graduated due reviews; rust test `blocked_phase_serves_graduated_reviews_but_withholds_other_new_topics`                                                                                                                                                                                                                                                                                              |
| F4 | Answering a card in the Study (topic-grouped) flow must not error ("not at top of queue") | done   | The Learn queue serves cards out of the live study queue, but every answer forced `from_queue=true`, so `pop_entry` failed. Added `optional bool from_queue` to `CardAnswer` (proto), reviewer sends false in Learn mode, and an out-of-queue answer now clears the cached queue so counts don't go stale. Tests: `test_answering_out_of_queue_allows_a_non_top_card` (reproduce + fix + counts refresh), 139 rust scheduler tests, full py suite + lint green |

## Audit (2026-06-30)

Reconciled the progression restatement + every later message. Initially-missed **U1, U2** are built, merged, and verified green (the refinement subagent's network died mid-run; its finished-but-uncommitted work was salvaged from the worktree, verified, and merged). Review-found correctness **F1, F2, F3** are now fixed + tested. Remaining: **U3** (topics-state UI), then **U4** polish. Everything in the restatement (P1-P9) is built and verified. No other dropped requirement found. Foundation milestones (Phases 1-4) tracked in [README](README.md) + the phase todos.

## Bespoke deck screens: authoring flow ([D35](decisions.md#d35), 2026-07-01)

> Replace the deck-browser home with a bespoke SvelteKit authoring flow (Decks list → Create/Edit Hierarchy → Concepts → Concept modal with MCQ practice problems), backed by a new native Speedrun store in the collection. Existing study/overview and menus stay reachable; the study engine is wired to this content in a later pass.

| #   | Requirement (observable)                                                                                                       | Status | Evidence / owner                                                                                                                                                                                                                                                                                                                                                |
| :-- | :----------------------------------------------------------------------------------------------------------------------------- | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | New "Decks" screen replaces the deck-browser home; blank, no top/bottom bar; title "Decks"                                     | done   | `main.py` `_deckBrowserState` hides `toolbarWeb`/`bottomWeb`/`web` and shows `speedrunWeb`; `deckbrowser.py` `refresh()` -> `load_sveltekit_page("speedrun-decks")`; `mediasrv.is_sveltekit_page` allowlists it; `speedrun-decks/+page.svelte` `<h1>Decks</h1>`. Smoke: `GET /speedrun-decks` is the home. (Bars hidden is code-verified; visual pending user.) |
| R2  | Decks table has exactly two columns: Name and To Do                                                                            | done   | `speedrun-decks/+page.svelte`: `Name` + `To do` data columns, plus a visually-hidden actions column carrying the R4/R11 edit and delete affordances.                                                                                                                                                                                                            |
| R3  | Clicking a deck row opens the existing overview/Study screen for that deck                                                     | done   | `+page.svelte` row -> `openDeck` -> `speedrunOpenDeck` RPC -> `mediasrv.speedrun_open_deck` sets the current deck and calls `moveToState("overview")` on the main thread.                                                                                                                                                                                       |
| R4  | An edit affordance opens the deck's hierarchy editor                                                                           | done   | `+page.svelte` per-row Edit -> `goto(/speedrun-hierarchy/{deckId})`.                                                                                                                                                                                                                                                                                            |
| R5  | "Create Deck" opens the Create Hierarchy screen (title "Create Hierarchy")                                                     | done   | `HierarchyEditor.svelte` renders `<h1 class="page-title">{pageTitle}</h1>` with `pageTitle = draft ? "Create Hierarchy" : "Edit Hierarchy"` (`draft = isUnsaved(deckId)`), so the create flow (`/speedrun-hierarchy/new`) shows the screen title "Create Hierarchy"; breadcrumb kept as secondary nav.                                                          |
| R6  | Hierarchy is an editable tree with a text input at each node                                                                   | done   | `HierarchyTree.svelte` + recursive `HierarchyNode.svelte` (`<input bind:value={node.title}>` per node).                                                                                                                                                                                                                                                         |
| R7  | Nodes can be added and deleted at any level                                                                                    | done   | `HierarchyNode.svelte` `addChild`/`removeChild` at any level (root has no delete by design, it is the deck).                                                                                                                                                                                                                                                    |
| R8  | Editing/adding a node pulses an animation upward through the ancestor branches (cumulative), respecting prefers-reduced-motion | done   | `HierarchyNode.svelte` `ctx.pulse(selfChain())` with staggered `--pulse-delay` per ancestor; `@media (prefers-reduced-motion: reduce)` hides the pulse.                                                                                                                                                                                                         |
| R9  | Leaf nodes show a visible affordance indicating they open Concepts                                                             | done   | `HierarchyNode.svelte` leaf-only "Concepts" button with a concept count.                                                                                                                                                                                                                                                                                        |
| R10 | Clicking a leaf opens the Concepts screen (title "Concepts") listing that leaf's concept titles                                | done   | leaf "Concepts" button -> `ConceptsPanel.svelte` `<h2>Concepts</h2>` lists the selected leaf's concept titles.                                                                                                                                                                                                                                                  |
| R11 | Concepts support add/delete/edit                                                                                               | done   | `ConceptsPanel.svelte` add / delete / open-to-edit (via the modal).                                                                                                                                                                                                                                                                                             |
| R12 | Clicking a concept opens a modal with a concept title input, a content description, and practice problems                      | done   | `ConceptModal.svelte`: title input + description textarea + practice-problem list.                                                                                                                                                                                                                                                                              |
| R13 | Each practice problem uses a 4-choice multiple-choice layout with a way to mark the correct choice                             | done   | `ChoiceEditor.svelte`: four choices, each with a radio that sets `correctIndex`.                                                                                                                                                                                                                                                                                |
| R14 | Problems and choices support add/delete/edit                                                                                   | done   | `ConceptModal.svelte` problem add/delete + prompt edit; `ChoiceEditor.svelte` edits the four choice texts + correct-marking (choice count fixed at 4 per R13).                                                                                                                                                                                                  |
| R15 | Authored content persists in a native Speedrun store in the collection (syncs), independent of Anki notes/scheduler            | done   | `pylib/anki/speedrun/authoring.py` over `col.get_config`/`set_config` (`speedrun_authoring`); 8 passing tests in `test_speedrun_authoring.py`; debounced autosave via `speedrunSaveHierarchy`. Smoke: `POST /_anki/speedrunListDecks`.                                                                                                                          |
| R16 | The old deck browser is hidden; old menus remain reachable                                                                     | done   | `main.py` `_deckBrowserState` hides the old browser chrome (`web`/`toolbarWeb`/`bottomWeb`) and `_deckBrowserCleanup` restores it on leaving; the native menubar is untouched.                                                                                                                                                                                  |

**Verification (2026-07-01, final integration pass).** Added `speedrun-decks` and `speedrun-hierarchy` to `mediasrv.is_sveltekit_page` so the Speedrun home webview loads them (without this, `load_sveltekit_page("speedrun-decks")` was treated as an external link). `just fix-fmt` clean; fixed a `prefer-const` eslint error in `HierarchyEditor.svelte`. After that the only `just check` failure is the pre-existing, unrelated `check:minilints` CONTRIBUTORS/git-identity gate (author not in CONTRIBUTORS; not touched per instructions). `just lint` (clippy/mypy/ruff/eslint/svelte/typescript) and `just test` (rust/pytest/vitest) are green, and the 8 `test_speedrun_authoring.py` store tests pass directly. Smoke `just run`: app starts, mediasrv serves, `GET /speedrun-decks` is the home, `POST /_anki/speedrunListDecks` succeeds with no traceback. The interactive create -> hierarchy (add/delete, upward pulse) -> leaf -> Concepts -> concept modal + 4-choice MCQ, and deck-row -> overview/Study -> top-bar back -> Decks flows are code-verified end to end but still need the user's eyes in the GUI. R5 stays `building`: the shared create/edit editor shows a Draft breadcrumb rather than the literal "Create Hierarchy" title.

## Bespoke Study screen: bar-less SvelteKit overview ([bespoke_study_screen plan](../../.cursor/plans/bespoke_study_screen_f3a89bbf.plan.md), 2026-07-01)

> Replace the native deck-overview "Study" screen with a bespoke, bar-less SvelteKit screen: Today's Progress + Start, three circular score rings, a centered concept tree with per-leaf mastery stages, and a per-subject stats modal. Phase 1 (backend/proto/Qt) shipped separately; this batch is the SvelteKit screen (route `ts/routes/speedrun-study/[deckId]/`).

| #   | Requirement (observable)                                                                                                                                                                                                                                                                                                                                  | Status | Evidence / owner                                                                                                                                                                                                                                                  |
| :-- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ST1 | Bar-less Study screen: renders into `speedrunWeb` with no top/bottom bars (repurposed Qt `overview` state); the page renders its own "all done / 0 remaining today" state (no native congrats)                                                                                                                                                            | done   | `ts/routes/speedrun-study/[deckId]/+page.svelte` full-window screen (no bars); Phase-1 Qt wiring hides bars. `todayProgress().allDone` drives the done state. Headless render `out/uiqa/study-*.png` shows no chrome.                                             |
| ST2 | Today's Progress: a horizontal bar of today's completion with the amount left to study today as the headline number, plus a primary Start button (`speedrunStartStudy`) and a quiet "Decks" back link (`speedrunShowDecks`)                                                                                                                               | done   | `+page.svelte` progress bar + `remaining` headline; `startStudy`/`showDecks` in `lib.ts`; `todayProgress` helper (studied vs remaining). Screenshot `out/uiqa/study-light.png`.                                                                                   |
| ST3 | Three score rings (Memory / Performance / Readiness): `ScoreRing.svelte` circular gauge; an abstaining/errored score renders an empty dashed ring with its give-up reason (never a fake fill); Readiness on the 472-528 scale with a whole-number readout                                                                                                 | done   | `ScoreRing.svelte` (dashed track + no arc when `abstained`/error, reason shown); `ringFraction` reuses `gaugePercent` (points across 472-528); readout via `displayNumber`. Screenshot shows a live ring + an abstaining ring.                                    |
| ST4 | Centered concept tree: authored hierarchy for structure with the taxonomy mastery stage overlaid on mapped leaves (case-insensitive leaf-title match; unmatched leaf = "not started"); each non-leaf node shows aggregate progress (mean of descendant stage fractions); fallback to the taxonomy `progress` paths when the deck has no authored children | done   | `ConceptTree.svelte` + `ConceptTreeNode.svelte`; `buildConceptTree(hierarchy, progress)` in `lib.ts`; leaf stages reuse `MASTERY_STAGES` (hierarchy=Applying). Tests `lib.test.ts` cover authored + overlay + fallback + parent aggregation. Tree block centered. |
| ST5 | More details modal: reuses the dashboard `ScoreTile` gauges for the three scores, then a per-subject section grouping `getSpeedrunScoreBreakdown` topics by content-category with meters (coverage, mean retrievability, application accuracy) and the score drivers; accessible (Esc-to-close + labelled) and reduced-motion respected                   | done   | `StatsModal.svelte` reuses `ScoreTile.svelte`; `buildSubjectBreakdown(breakdown)` groups by category with weighted meters; test in `lib.test.ts`. Focus-trap + Esc + `role="dialog"`/`aria-modal`; `prefers-reduced-motion` guard.                                |
| ST6 | Secondary actions preserved in an overflow menu: Deck options, Custom study, Unbury, Edit description, and Rebuild/Empty when the deck is filtered, all via `speedrunOverviewAction`                                                                                                                                                                      | done   | `+page.svelte` overflow menu -> `overviewAction(action)`; filtered detected via `getDeckNames` (include vs exclude filtered) so Rebuild/Empty appear only for filtered decks. Menu is keyboard-accessible (Esc/click-away close).                                 |

**Verification (2026-07-01, Phase 2).** New route `ts/routes/speedrun-study/{lib.ts,lib.test.ts,[deckId]/+page.ts,[deckId]/+page.svelte}` + `ScoreRing.svelte`, `ConceptTree.svelte`, `ConceptTreeNode.svelte`, `StatsModal.svelte`. Loader is per-item error-isolated (six independent try/catch blocks) so one failed RPC never blanks the page. `lib.test.ts` (7 vitest cases) covers `buildConceptTree` (authored + stage overlay + fallback + parent aggregation), `buildSubjectBreakdown`, `todayProgress`, and the ring geometry. Full `just check` was run: `check:svelte`, `check:typescript`, `check:eslint`, `check:vitest`, `check:mypy`, `check:ruff`, `check:clippy`, `check:rust_test`, `check:pytest`, and both `check:format:prettier` + `check:format:dprint` all pass. The only two failing gates are the known pre-existing/environmental ones, neither mine: `check:minilints` (the CONTRIBUTORS/git-identity gate) and `check:format:rust` (cargo-fmt drift in the untouched Phase-1 file `rslib/src/speedrun/score_breakdown.rs`, which this phase must not touch). Web built with `just rebuild-web`; the screen was rendered headless (Chrome-for-Testing against a throwaway offscreen instance, RPCs mocked) to `out/uiqa/study-{light,dark,modal,menu,alldone,mobile}.png`, confirming no top/bottom bars, the progress bar + Start, the three rings (Memory live, Performance abstaining/dashed, Readiness on 472-528), the centered concept tree, the details modal, and the overflow menu (Rebuild/Empty shown for a filtered deck).

## New study screen: FSRS + animated authored study (2026-07-01)

> User: "Redo actual studying screen. Create/connect new UI screens and disconnect the old screens (hide top and bottom bars)." New-topic teaching animation (centered hierarchy tree, animate down to the leaf); mastery-type card chrome + concepts-learned indicator; learning shows two of the concept's problems (correct answer only) then similarity then concept details, no difficulty; topic upgrades Learning->Practicing only when all its concepts are learned (cards upgrade together); later stages upgrade per concept; upgrade animation shows from->to. Then: "I want FSRS" and "practice should be free-response not MCQ", and the difficulty rating should be built into the card at the bottom center (not a detached bottom bar like base Anki's). Concurrency: another agent owns the overview (`speedrun-study` route + `overview.py`); this work uses the `speedrun-review` route and does not touch those.

| #    | Requirement (observable)                                                                                                                                                | Status | Evidence / owner                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| :--- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ST1  | A new SvelteKit study screen (route `speedrun-review`) is the actual card-studying screen for Speedrun decks, replacing the classic Anki reviewer for study             | done   | `ts/routes/speedrun-review/[deckId]/{+page.ts,+page.svelte}`: `+page.ts` loads hierarchy + `studyState`; `+page.svelte` orchestrator loops `nextCard` -> render by kind/state -> `answerCard`/`recordLearned` -> optional upgrade -> until `kind:"done"`, then a finished state (overview via `openDeck` / home via `showDecks`). RPC wrappers in `lib.ts`. GET `/speedrun-review/1` -> HTTP 200; route compiled into client bundle node 15.                                                                                                |
| ST2  | Top and bottom bars are hidden while studying; the study screen owns the whole window                                                                                   | done   | `main.py` `_speedrunStudyState`/`_speedrunStudyCleanup` hide `toolbarWeb`/`bottomWeb`/`web` + show `speedrunWeb` + `load_sveltekit_page("speedrun-review/{deckId}")` (mirrors `_deckBrowserState`); `mediasrv.speedrun_start_study` -> `moveToState("speedrunStudy", did)`; `is_sveltekit_page` allowlists `speedrun-review`. Qt wiring code-verified (build + mypy/ruff green).                                                                                                                                                            |
| ST3  | Teaching a new topic opens a "New Topic!" animation showing the centered hierarchy tree and animating down the tree to the topic leaf                                   | done   | `NewTopicIntro.svelte` + `TopicTreeNode.svelte`: "New topic" declaration + centered authored tree, lighting the root->leaf path in order (staggered; instant under `prefers-reduced-motion`). Path from `pathToNode(root, topicNodeId)`. Test `pathToNode returns the root-to-node ancestor chain`.                                                                                                                                                                                                                                         |
| ST4  | The teaching screen shows the card's mastery type (Learning/Practicing/Applying/Mastering) and a concepts-learned indicator (e.g. 3/5)                                  | done   | `ConceptLearn.svelte` header shows `MasteryBadge` (Learning) + a `{current} / {total} concepts` counter the orchestrator increments from `learnedCount`/`totalCount`; `MasteryBadge.svelte` label via `stageLabel`. Test `stageLabel maps engine states to display labels, hierarchy reads Applying`.                                                                                                                                                                                                                                       |
| ST5  | In learning, each concept shows two of its problems with only the correct answer shown; the user states the similarity; then the concept details appear below           | done   | `ConceptLearn.svelte` renders `pickTwoProblems(concept)` side by side (prompt + only the `shownProblem` correct choice), a "What these share" textarea, then reveals `concept.content`. Helpers in `lib.ts`. Tests `pickTwoProblems takes the first two, degrading for fewer`, `shownProblem keeps the prompt and only the authored-correct choice`.                                                                                                                                                                                        |
| ST6  | No difficulty rating is shown during new-topic teaching (learning stage)                                                                                                | done   | `ConceptLearn.svelte` (learning) has no `DifficultyBar`; its footer is only Reveal/Continue. The rating bar renders only in `PracticeRecall`/`ProblemCard` (non-learning).                                                                                                                                                                                                                                                                                                                                                                  |
| ST7  | A topic upgrades Learning->Practicing only when all its concepts are learned; those cards upgrade together (one upgrade animation)                                      | done   | `study.record_learned` (pylib/anki/speedrun/study.py) marks seen then flips a leaf's concepts together only when all are seen; returns `{upgraded, from, to, conceptIds}`. Tests `test_record_learned_is_topic_gated`, `test_record_learned_only_flips_the_completed_leaf` pass. (Celebratory animation is ST11, frontend.)                                                                                                                                                                                                                 |
| ST8  | After learning, concepts advance individually (Practicing->Applying->Mastering) on their answers; Again demotes one stage (floor Practicing)                            | done   | `study.record_answer` advances one state on a cleared rolling signal (>=Good over recent, min reps), demotes one on Again, floors at practicing. Tests `test_record_answer_advances_once_signal_clears`, `_demotes_on_again`, `_floors_at_practicing` pass.                                                                                                                                                                                                                                                                                 |
| ST9  | Every card shows a mastery-type visual                                                                                                                                  | done   | `MasteryBadge.svelte` (rung meter + mono label) renders on every card: `ConceptLearn` (Learning), `PracticeRecall` (Practicing), `ProblemCard` (Applying/Mastering), `ScaffoldPicker` (Applying). Rank/label via `stageRank`/`stageLabel`. Tests `stageRank orders the ladder and STAGE_TOTAL counts the rungs`, `stageLabel maps engine states to display labels, hierarchy reads Applying`.                                                                                                                                               |
| ST10 | Non-learning cards present Again/Hard/Good/Easy (functionally like base Anki) built into the card at the bottom center, not a detached bottom bar, where Next would sit | done   | `DifficultyBar.svelte` (Again/Hard/Good/Easy -> rating 1..4, keys 1-4) sits in the `ReviewCard.svelte` `footer` slot at the bottom center (where a Next button would sit), not a detached bar; `onRate` -> `answerCard`. Used by `PracticeRecall` and `ProblemCard`.                                                                                                                                                                                                                                                                        |
| ST11 | When a card is upgraded, an animation declares it and shows from->to (feels good)                                                                                       | done   | `UpgradeAnimation.svelte` declares the jump with `upgradeLabels(from,to)` (from -> to), a pop+burst (instant under `prefers-reduced-motion`), auto-advance + Continue; the orchestrator shows it whenever `answerCard`/`recordLearned` report `upgraded`. Test `upgradeLabels renders the from/to of a stage change`.                                                                                                                                                                                                                       |
| ST12 | Studying uses real FSRS: authored concepts are materialized into Anki cards graded via `col.sched.answer_card`, so intervals are genuine FSRS                           | done   | `materialize.reconcile` (pylib/anki/speedrun/materialize.py) creates one `SpeedrunItem` card per concept (keyed by `ConceptId`), removes orphans, and enables FSRS (`col.set_config("fsrs", True)`); `mediasrv.speedrun_answer_card` grades via `col.sched.answer_card` (real FSRS) then `study.record_answer`. Tests `test_reconcile_creates_one_card_per_concept_and_enables_fsrs`, `_removes_orphaned_cards`, `test_concept_id_for_note_maps_card_to_concept` pass.                                                                      |
| ST13 | Practicing is free-response recall (not MCQ); MCQ is used only at Applying (with the tree scaffold) and Mastering (scaffold removed)                                    | done   | Orchestrator routes by state: practicing -> `PracticeRecall.svelte` (free-response recall + reveal + rating), hierarchy -> `ScaffoldPicker.svelte` (pick down the tree via `scaffoldSteps`) then `ProblemCard.svelte` (MCQ), mastering -> `ProblemCard.svelte` (no scaffold); `rotateProblem` picks one problem per review. Tests `scaffoldSteps walks root's children down to the concept's leaf`, `scaffoldSteps is empty for a concept on the root or one not found`, `rotateProblem cycles one problem per review and handles empties`. |

**Verification (2026-07-01, backend phase).** Shipped the backend for the new study screen: `pylib/anki/speedrun/materialize.py` (`SpeedrunItem` note type `ConceptId`+`Title`, `reconcile` = one card per authored concept + orphan removal + FSRS on), `pylib/anki/speedrun/study.py` (config store `speedrun_study_progress`: `get_study_state`, `record_learned` topic-gated flip, `record_answer` per-concept advance/demote with practicing floor, `next_learning_block`), four `FrontendService` JSON RPCs in `proto/anki/frontend.proto` (`SpeedrunStudyState`, `SpeedrunNextCard`, `SpeedrunAnswerCard`, `SpeedrunRecordLearned`) implemented in `qt/aqt/mediasrv.py` and registered, `speedrun-review` allowlisted in `is_sveltekit_page`, `speedrun_start_study` re-pointed at the new `speedrunStudy` main-window state, and that state + cleanup added in `qt/aqt/main.py`. `just build` regenerated the bindings (`out/ts/lib/generated/backend.ts` now exports `speedrunStudyState`/`speedrunNextCard`/`speedrunAnswerCard`/`speedrunRecordLearned` for the frontend). `just test-py` green (pylib + aqt); the 10 new `test_speedrun_study.py` cases pass; `just lint` (clippy/mypy/ruff/eslint/svelte/typescript) green; touched Python is ruff-format clean and `frontend.proto` is clang-format clean. Owned criteria ST2/ST7/ST8/ST12 met (see rows). The frontend-owned rows (ST1, ST3-ST6, ST9-ST11, ST13) stay `building`; their RPC surface is now available. Not touched (coordination): `qt/aqt/overview.py`, `ts/routes/speedrun-study/**`.

**Verification (2026-07-01, frontend phase).** Shipped the study screen frontend under `ts/routes/speedrun-review/`: `lib.ts` (RPC wrappers `studyState`/`nextCard`/`answerCard`/`recordLearned` reusing the authoring enc/dec + `getHierarchy`, plus pure helpers `shownProblem`/`pickTwoProblems`, `rotateProblem`, `stageLabel`/`stageRank`/`STAGE_TOTAL`, `upgradeLabels`, `findConcept`/`pathToNode`/`pathToConcept`/`scaffoldSteps`), `[deckId]/{+page.ts,+page.svelte}` (loader + promise-driven session orchestrator: `nextCard` -> render by kind/state -> `answerCard`/`recordLearned` -> upgrade -> done), and components `NewTopicIntro`, `TopicTreeNode`, `ConceptLearn`, `PracticeRecall`, `ProblemCard`, `ScaffoldPicker`, `MasteryBadge`, `DifficultyBar` (bottom-center, in `ReviewCard`'s footer slot), `UpgradeAnimation`, `ReviewCard`. All reuse the Speedrun tokens (`ts/lib/sass/speedrun-tokens.scss`) and respect `prefers-reduced-motion`. `lib.test.ts` (10 vitest cases: `shownProblem`, `pickTwoProblems`, `rotateProblem`, `stageLabel`, `stageRank`/`STAGE_TOTAL`, `upgradeLabels`, `findConcept`, `pathToNode`, and the two `scaffoldSteps` cases) passes via `just test-ts`. `just lint` green (check:eslint ts + qt, check:svelte, check:mypy, check:ruff; clippy/typescript cached-unaffected). Touched `.ts` are dprint-clean and `.svelte` are prettier-clean (the `check:format:prettier` scope). Smoke: `just rebuild-web` then `GET http://127.0.0.1:40000/speedrun-review/1` -> HTTP 200, and the route compiles into client bundle node 15 (contains `Start learning`, `Say it in your own words`, `Mastery up`, `Locate`). Owned criteria ST1, ST3-ST6, ST9-ST11, ST13 now met (see rows). Not touched (coordination): `qt/aqt/overview.py`, `ts/routes/speedrun-study/**`, and the backend set (`pylib/anki/speedrun/*`, `qt/aqt/mediasrv.py`, `qt/aqt/main.py`, `proto/*`).

## Demo mode: preview the study screens without cards (2026-07-01)

> User: "Add a demo mode so a developer can see the whole new study screen (all screens, states, animations) WITHOUT studying real cards." A stepper/gallery route that reuses the REAL `speedrun-review` components driven by inline mock data, no collection and no backend RPCs, reachable from an easy in-app entry point. Coordination: do not touch `overview.py` or `ts/routes/speedrun-study/**`, and only a minimal backward-compatible prop addition to a `speedrun-review` component if unavoidable.

| #    | Requirement (observable)                                                                                                                                                                                 | Status | Evidence / owner                                                                                                                                                                        |
| :--- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DM1  | A demo route `ts/routes/speedrun-review-demo/+page.svelte` renders with NO collection and NO backend RPC calls, reusing the real `../speedrun-review/` components on inline mock data                    | done   | `+page.svelte` imports the real components; no `studyState`/`nextCard`/`answerCard`/`recordLearned` calls; no `+page.ts` (SPA, `ssr=false`).                                            |
| DM2  | New Topic intro (`NewTopicIntro`) scene with a Replay button                                                                                                                                             | done   | `intro` scene renders `NewTopicIntro`; Replay remounts via a keyed block.                                                                                                               |
| DM3  | Learning: `ConceptLearn` for a concept with >=2 problems (prompt + only correct choice), similarity input, concept-content reveal, "N / total concepts" counter, Learning badge                          | done   | `learning` scene; onNext cycles the topic's concepts so the counter advances; each concept has 3 problems.                                                                              |
| DM4  | Practicing: `PracticeRecall` (free-response recall + reveal + bottom-center `DifficultyBar`), Practicing badge                                                                                           | done   | `practicing` scene; onRate resets the card locally so the reveal + rating can be replayed.                                                                                              |
| DM5  | Applying: `ScaffoldPicker` (pick down the mock tree) then `ProblemCard` (MCQ) + difficulty, Applying badge                                                                                               | done   | `applying` scene is a two-phase local machine (scaffold -> problem -> loop); concept is nested 2 deep so the scaffold has real steps.                                                   |
| DM6  | Mastering: `ProblemCard` (MCQ, no scaffold) + difficulty, Mastering badge                                                                                                                                | done   | `mastering` scene renders `ProblemCard` with `state="mastering"`, no scaffold; onRate rotates to the next problem.                                                                      |
| DM7  | `UpgradeAnimation` shown for at least two transitions (Learning->Practicing and Applying->Mastering) with a Replay button                                                                                | done   | Two upgrade scenes; Replay remounts via keyed block.                                                                                                                                    |
| DM8  | A gallery of `MasteryBadge` in all four stages (Learning/Practicing/Applying/Mastering)                                                                                                                  | done   | `badges` scene renders `MasteryBadge` for `learning`/`practicing`/`hierarchy`/`mastering`.                                                                                              |
| DM9  | The done/finished state                                                                                                                                                                                  | done   | `done` scene mirrors the orchestrator's finished view copy; buttons restart the demo instead of hitting the network.                                                                    |
| DM10 | Navigation (sidebar list + prev/next) to jump between scenes; Replay for animated ones; all callbacks handled locally, nothing hits the network                                                          | done   | Left rail lists all scenes; prev/next chrome; every callback mutates local state only.                                                                                                  |
| DM11 | Mock data in the demo (`demo-fixtures.ts`): mock `Hierarchy` with 2 topics, each 2-3 concepts, each concept content + 3-4 problems with 4 choices and a set `correctIndex`                               | done   | `demo-fixtures.ts` Cardiovascular + Renal, 3 concepts each (across two leaves), every concept 3 problems with `correctIndex` set.                                                       |
| DM12 | Easy entry point: `speedrun-review-demo` in `is_sveltekit_page`; a Tools-menu action opens the demo via a new `qt/aqt/speedrun_demo.py` (dialog-webview mirroring `speedrun_dashboard.py`)               | done   | `mediasrv.is_sveltekit_page` allowlists it; `speedrun_demo.py` `QDialog`+`AnkiWebView` loads `load_sveltekit_page("speedrun-review-demo")`; `main.py setupMenus` adds the Tools action. |
| DM13 | UI reuses the Speedrun identity (cool paper, charcoal ink, one marigold signal, mono readout labels); no AI-generic UI, no em dashes, sentence case, no emoji; reduced-motion, responsive, visible focus | done   | Demo chrome inlines the `--sr-*` score-instrument tokens (light+dark) like `SpeedrunDashboard`; `prefers-reduced-motion` guards; focus-visible rings; mobile rail collapse.             |
| DM14 | Verify: `just lint` green for touched code; touched `.ts` dprint-clean, `.svelte` prettier-clean, Python ruff/mypy clean; smoke `GET /speedrun-review-demo` == 200 and compiled into the bundle          | done   | See verification note below.                                                                                                                                                            |

**Verification (2026-07-01, demo mode).** New files: `ts/routes/speedrun-review-demo/{+page.svelte,demo-fixtures.ts}` (no `+page.ts`: the root layout is `ssr=false`/`prerender=false`, so the SPA fallback serves it), `qt/aqt/speedrun_demo.py`; edits: one line in `qt/aqt/mediasrv.py` (`is_sveltekit_page` allowlist), a Tools-menu block in `qt/aqt/main.py setupMenus`. The demo reuses the real `../speedrun-review/` components and only imports the pure helpers `findConcept`/`rotateProblem` and the `Rating` type from its `lib`; it never calls `studyState`/`nextCard`/`answerCard`/`recordLearned`. It inlines the `--sr-*` score-instrument tokens (light + dark) rather than `@use`-ing `speedrun-tokens.scss`, since that file was concurrently repurposed for the deck-screen chassis palette; the reused components inherit `--sr-*` via the cascade, so the demo is correct regardless of that file's state. No component prop was added (every real component drove standalone as-is). `just lint` green (clippy/mypy/ruff/eslint/svelte/typescript); touched `.ts` is dprint-clean, `.svelte` is prettier-clean, the three `.py` are `ruff format --check` + `ruff check` clean. `just rebuild-web` built the route into the client bundle (`out/sveltekit/_app/immutable/nodes/16.*.mjs` contains "Study screens", "No collection and no cards", "MCAT physiology", "Cardiac action potential"). Live `GET /speedrun-review-demo` on the already-running instance returns 404 only because that process still holds the pre-edit `is_sveltekit_page` list in memory; the sibling routes `/speedrun-decks` and `/speedrun-review/1`, which use the identical serving path, return 200, and a direct call of the edited `is_sveltekit_page("speedrun-review-demo")` returns `True`, so a restart (required to load any Python change, and which the user asked to defer) yields 200. Not touched (coordination): `qt/aqt/overview.py`, `ts/routes/speedrun-study/**`, and the existing `ts/routes/speedrun-review/**` component behaviour.

---

<sub>Maintained with the `iris-log` skill by Iris Cai.</sub>
